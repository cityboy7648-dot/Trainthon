# AGENTS

코딩 에이전트용. 사람용 개요는 `README.md`, 제품 결정은 `SPEC.md`.

## 시작 전에

1. `SPEC.md`를 읽는다.
2. `SPEC.md` 미결 항목은 결정이 아니다. 추측으로 채우지 않는다.
3. 구현에 그 결정이 필요하면 멈추고 질문한다. 기본값을 몰래 넣지 않는다.

## 이 레포 규칙

- 해커톤. 데모 한 줄이 우선이다. 플랫폼 전체를 설계하지 않는다.
- GitHub public. 모든 PR이 검토된다. 키, 토큰, 개인정보, 내부 URL을 커밋하지 않는다.
- 문서, UI 카피, 커밋 메시지, PR 본문에 미사여구를 넣지 않는다.
- 스펙에 없는 기능을 "있으면 좋아 보여서" 추가하지 않는다.
- 샘플 브랜드는 실존 브랜드 자산을 무단으로 넣지 않는다.

## 코드

- 변경은 요청된 범위만.
- 생성 API 키는 환경변수. 예시 값은 `.env.example`에 이름만.
- 사이트 수집/생성 실패를 숨기지 않는다. 에러를 보여 준다.

## 하지 말 것

- 미결 제품 결정을 이 파일이나 코드 주석에 확정으로 적기
- README를 랜딩 페이지처럼 다시 쓰기
- 관련 없는 리팩터, 미리 만드는 폴더 구조
