# SPEC

해커톤. GitHub public. PR은 전부 공개 검토된다.

제품이 뭔지만 이 파일에 적는다. 어떻게 만들지는 `IMPLEMENTATION.md`.

## 무엇을 만드는가

웹/앱/이커머스 사이트 URL을 넣는다. 사이트의 브랜드를 읽는다. 사용자가 방향을 고른다. 그다음을 만든다.

- 브랜드 에셋
- 인스타에 바로 올릴 수 있는 홍보물
- AI 영상

전부 자동이 아니다. 방향은 사용자가 정한다.

## 플로우

1. 사이트 URL 입력
2. 브랜드 추출
3. 사용자가 방향 선택
4. 산출물 생성

## 제약

- 해커톤. 데모 한 줄이 우선이다.
- public 레포. 시크릿을 커밋하지 않는다.
- 문서/UI/커밋에 미사여구를 넣지 않는다.
- `SPEC.md`에 없는 제품을 만들지 않는다.
- 구현 상세는 `IMPLEMENTATION.md`에만 적는다. 여기 적지 않는다.
